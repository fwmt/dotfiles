# Dotfiles

Personal shell/editor/git configuration managed outside project repositories.

## Bootstrap

Run from this repository root:

```sh
./install.sh
```

This is compatible with GitHub Codespaces dotfiles auto-bootstrap (`install.sh`).
